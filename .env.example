NODE_ENV=production
HOST=0.0.0.0
PORT=4000
STOCKFLOW_SECURE_COOKIES=true

# Required for free online hosting because Render Free has no persistent disk.
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_SSL=require

# Local/LAN only fallback.
# STOCKFLOW_DATA_DIR=./data
